node_modules/
dist/
bundle/
*.tgz
*.tsbuildinfo
.DS_Store
coverage/
*.log
.roster-tmp/
docs/verification/tmp/
dist-site/

# accidental review/validation artifacts (never commit)
actionlint
docs/lab/tmp-*/
# Local publish registries write auth tokens here; package-local credentials
# must never enter source control (the root .npmrc remains available for policy).
packages/*/.npmrc

# generated into the CLI package at pack time (single source of truth: /README.md)
packages/cli/README.md

# vitest CI reporter output
test-results.junit.xml
packages/cli/LICENSE
