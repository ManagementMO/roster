# Shot list — real asset pass

The active composition is 12.4 seconds. Frame ranges use the 60 fps master; the transitions overlap in time but each frame is owned by one scene draw path. Fast entrances finish before the next beat, then hand off after a short readable settle rather than sitting on a finished state.

| Beat | Time | Master frames | Visual action | Audio cue |
| --- | --- | ---: | --- | --- |
| Hook | 00:00.00–00:01.35 | 0–81 | Paper-white typography lands `YOUR AGENT HAS / 200 / TOOLS.` and then `AND THEY'RE POLLUTING YOUR CONTEXT.`; the claim now holds long enough to read before the supplied Roster mark seeds the terminal handoff. | Opening impact and restrained ticks. |
| Terminal push | 00:01.35–00:04.55 | 81–273 | A macOS-like terminal enters in perspective. It types the complete `npx roster init` and `roster sync --five` transcript, settles the final line, then hands off to the tool field. | Keyboard-like ticks, confirmation chimes, and a leftward whoosh. |
| Focus | 00:04.55–00:07.35 | 273–441 | Terminal depth hands off to a clean shortlist rail: one task chip, five real-mark candidates, one neutral glass fit beam, and a single `RIGHT FIT` focus state. The rail resolves `5 / 200`, then hands off. | Mixkit beat continues under five selection pops and a short resolved hold. |
| Ranked local route | 00:07.35–00:10.30 | 441–618 | `verify checkout flow` enters as an agent intent. Three real capabilities are visibly ranked; Playwright locks as `RANK 01 / FIT 96`, receives the active route, and returns `2 assertions found` at `checkout.spec.ts:42`. | Request intake, ranking ticks, selected-route pulse, and a concise result impact. |
| Identity | 00:10.30–00:12.40 | 618–744 | The selected-tool energy hands into the rotating ecosystem orbit. The final card holds `ROSTER`, `YOUR AGENT HAS 200 TOOLS.`, `ONLY THE ONES THAT MATTER START.`, `npx roster init`, and local-first/open-source/MCP copy. | Final chord and controlled tail. |

Run `pnpm film:diffusion-stills` for entry/midpoint/exit QA captures; run `pnpm film:diffusion-contact` for the active sheet. The music source and usage note live in `assets/music/README.md`.
