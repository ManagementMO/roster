# Lab campaign digest — 2026-07-04 swarm run (wf_5f611d4a-ad9)

> 16 charters launched; 15 returned full reports (exp:e2e-realism died on a session limit, as did 58 of ~70 verifiers — findings below are UNVERIFIED unless a verdict is shown). Raw reports: results-<slug>.json · notes-<slug>.md.

## retrieval-minilm
- **shipped-draft-lex-vs-hybrid**: Hybrid 0.591/0.788/0.622 (hit@1/hit@5/MRR) vs lexical-only 0.439/0.621/0.460 — +15.2/+16.7/+16.2 pts, zero-overlap hit@5 0.125→0.500, at 0.36ms mean draft latency (vs 0.09ms lexical).
- **fusion-weight-sweep**: Quality rises monotonically with cos weight: shipped 0.3/0.7 = 0.591/0.788/0.622, best cell 0.15/0.85 = 0.636/0.833/0.649 (+4.5pt hit@1, +4.5pt hit@5, +2.7pt MRR); pure 0/1 = 0.636/0.833/0.634; paired per-need 0/1-vs-shipped RR wins 9-5, hit@5 wins 3-0.
- **cosine-span-and-abstain-threshold**: Abstain NEVER fired: 0/66 drafts (span p0=0.218, median 0.554, p100=0.935 — vs 0.041 in dense-live's 4-tool setup); hit@5 flat at 0.788 through threshold 0.30 then falls (0.773 at 0.40), hit@1 falls from 0.35 up — raising the gate only hurts.
- **hybrid-regressions-and-maxcos-gate**: 30 improved / 4 regressed / 32 unchanged (hit@5: 12 gains, 1 loss); all 4 regressions have best-acceptable cos ≤0.09 (one negative: −0.045); the maxCos gate is net-NEGATIVE at every t≥0.25 (t=0.3: hit@1 0.591→0.561, hit@5 0.788→0.742) — rejected.

**Findings:**
- [MEDIUM · CONFIRMED] Shipped 0.3/0.7 fusion weights leave measurable MiniLM quality on the table; optimum is ~0.15/0.85 *(proposes change)*
- [MEDIUM · CONFIRMED] The 0.15 cosine-span abstain gate never fires at realistic corpus scale — its documented calibration story is an artifact of tiny candidate sets *(proposes change)*
- [INFO · unverified] Dense rung verified live at corpus scale: hybrid beats lexical by ~16 points across all metrics for 0.4ms per draft
- [MEDIUM · CONFIRMED] MiniLM has a measurable blind-spot family (8/66 needs with correct-tool cosine ≈ 0) that causes all 4 hybrid regressions and no fusion tuning fixes it *(proposes change)*

**Surprises:** Cosine span is strongly corpus-size-dependent: 0.041 on dense-live's 4-tool set vs 0.218–0.935 on the 133-tool corpus — so the abstain mechanism the verification transcript 'proves' simply never engages at production scale. · Pure cosine (0/1) beats the shipped 30% lexical mix on every overall metric — BM25 is mostly ballast for MiniLM here — yet a small 0.1–0.15 lexical weight still beats 0/1, so lexical's residual value is tie-breaking, not ranking. · The obvious fix for hybrid regressions (abstain when max absolute cosine is low) is measurably net-negative at every threshold: the needs where dense is confidently weak are needs where lexical is even weaker. · MiniLM anti-correlates 'remember that the user prefers dark mode' with the correct memory tools (cos −0.045) while confidently ranking slack_get_user_profile — a semantic blind spot no weight/threshold tuning can fix, only OATS or a better model.

## gemma-calibration
- **gemma-retrieval-quality (a)**: Hybrid beats lexical hit@5 0.788 vs 0.621, hit@1 0.682 vs 0.439, MRR 0.687 vs 0.467; zero-overlap style hit@5 0.125→0.500, non-english 0.5→1.0, terse 0.923→1.0.
- **gemma-span-distributions (b)**: Every noise span exceeds 0.15: gibberish spans 0.2054–0.310, real spans 0.2275–0.605 — the distributions overlap (gibberish max 0.310 > real min 0.2275), so the gate never fires and no span value separates noise from signal.
- **gemma-threshold-sweep (c)**: t∈[0,0.20] is a dead zone (identical hit@5 0.788, 0% abstain, 100% gibberish engage); rejecting all gibberish requires t=0.35 which falsely abstains 28.8% of real needs and costs −7.6 pts hit@5; follow-up max-cosine gate also fails (90% gibberish rejection at t=0.50 costs 21.2% real abstain).
- **gemma-matryoshka-dims (d)**: hit@5 rises 0.742/0.788/0.803/0.818 (128/256/512/768) but hit@1 peaks at 256 (0.682 vs 0.667) and MRR is flat (0.687 vs 0.689); real spans shrink with dims (p50 0.403→0.302) so at 512/768 the 0.15 gate misfires on 1 real need — at 768 costing −1.5 pts hit@5 (0.818 gated vs 0.833 forced-engage).
- **gemma-regressions (e)**: 29 wins / 1 regression / 36 unchanged; the single regression ("show me what's inside config.yaml": fs__read_text_file lexical rank 3 → out of top-10) is model-level not truncation — Gemma ranks git__git_show first at every dim (pure-cos primary rank 6/23/29/15 at 128/256/512/768).
- **gemma-noise-consequence + timing (followup)**: Gibberish in Gemma mode returns confident dense rankings ("asdf jkl qwerty uiop zxcv" → playwright__browser_press_key cos 0.459) while lexical mode returns visibly signal-free score-0.00 recency fallback; warm load 555 ms, 19.9 ms/doc → 133-tool backfill ≈ 2.6 s.

**Findings:**
- [MEDIUM · CONFIRMED] The shipped 0.15 abstain threshold is inoperative for the default model (Gemma) — the noise-abstain protection does not exist in Gemma mode *(proposes change)*
- [MEDIUM · CONFIRMED] No span (or max-cosine) threshold can separate garbage from real needs on q8 Gemma — cosine-geometry noise gating is unsalvageable for this model *(proposes change)*
- [MEDIUM · ARTIFACT] Threshold and Matryoshka dims are silently coupled: at 512/768 the 0.15 gate fires ONLY on real needs (never noise) and costs quality *(proposes change)*
- [INFO · unverified] 256 is a defensible Matryoshka default: MRR parity and best hit@1, at −3.0 pts hit@5 vs native 768
- [INFO · unverified] With real Gemma the dense rung delivers large, nearly regression-free gains exactly where lexical is blind
- [LOW · unverified] The single hybrid regression is model-level Gemma confusion, not a truncation artifact
- [INFO · unverified] Gemma warmup is cheap once cached: 133-tool base-vec backfill costs ~2.6 s of inference

**Surprises:** Pure garbage never abstains: even the string '9481 2750 6613 0092 8837' produces a cosine span of 0.205 — above the 0.15 gate — and unicode junk 'ﬂ‡†¶• ∆˚¬… æœ∑´®†' reaches 0.279; the q8 Gemma embedding cone is so tight that noise geometry looks like signal geometry. · 'asdf jkl qwerty uiop zxcv' gets a semantically cute but spurious top tool: playwright__browser_press_key at cos 0.459 — garbage in, confident-looking keyboard-themed ranking out. · Spans SHRINK as Matryoshka dims grow (real p50 0.403@256 → 0.302@768), so the abstain threshold silently re-tunes itself with any dims change — and at 768 the only need it ever abstains on is a zero-overlap need, dense's home turf, costing a hit. · hit@1 peaks at 256 dims while hit@5 peaks at 768 — Matryoshka truncation is not monotonically worse; it slightly sharpens the top-1 while blurring the tail. · Word-shuffled needs are indistinguishable from real needs in span space (p50 0.384 vs 0.401) AND still retrieve well (hybrid hit@5 0.758) — 'noise' by word order is not noise to Gemma, which validates engaging on it; only gibberish is true noise and n=10 of it overlaps the real distribution anyway. · The derivation pipeline was provably identical to production: raw-native→truncate(256) matched the real TransformersEmbeddings provider bit-for-bit (maxAbsDiff 0.0), and the reimplemented sweep fusion reproduced real store.draftCandidates() top-10s with 0 mismatches across all 66 needs × 4 dims.

## oats-dynamics
- **baseline-hybrid-133x66**: Hybrid hit1 .606 / hit5 .788 / MRR .647; dense-only rank-1 36/66 beats lexical 26/66; cosine span min .2175 so 66/66 needs clear the 0.15 gate and hybrid order differs from lexical on all 66.
- **oats-sensitivity-curve**: A cliff, not a curve: N=1..3 zero movement (minPositives floor), at N=4 mean hybrid rank jumps 52.8/69.5/52.5 to 2.3/2.8/2.3 in one nightly, then saturates flat through N=12 (cos ~.22-.34).
- **oats-idempotence-drift**: Bit-identical idempotent: all 29 consecutive L2 deltas exactly 0 (runOats recomputes from base every time); single shot moves adj L2 .6733 / cos .7734 from base; hypothetical chained feedback converges geometrically (delta 6.4e-10 at step 30) to cos .9031 with the positive centroid, no oscillation.
- **oats-destructive-interference**: Poll-only learning pushed SCRAPE below baseline (rank 5.0 to 10.7, cos .382 to .324) because adj replaces base; dual-mode learning is ~2x worse than each single-mode optimum (SCRAPE 2.7 vs 1.3, POLL 7.0 vs 3.3).
- **oats-negatives-push-away**: Failures-only: adjusted=0 (beta never engages without >=4 positives), rank 6.0 to 6.0; realistic: wrong-tool dense 18.7 to 22.0 and hybrid 6.0 to 6.3 (cosmetic), right tool 21.0 to 21.0 (zero passive gain), suite hit5 .788 to .788 (no collateral, no fix).
- **oats-poisoning-and-recovery**: P<=3 nothing, P=4 flips #1 on 2/4 evals (poisoned rank 17.8 to 3.5, brave hit@1 1/4 to 0/4); recovery needs exactly 4 right-tool successes (R=4: hit@1 3/4, above its own 1/4 baseline); diverse failures are anti-monotone (rank 7.3 at F=1 but 5.0 at F=6) and 15 direct failures on the poisoned needs leave dense rank at 4 vs pre-poison 23.5.
- **beta-centroid-norm-mechanism**: meanVec norm shrink confirmed: negCentroid norm 1.0 (F=1) to .5547 (F=6) with cos-to-evals rising .2099 to .2584 in lockstep; identical-need repeats freeze at .2099 (zero added push); unit-normalized centroids flatten it (~.287-.292); positive side decays too (pos-centroid norm .6256 at N=4 to .469 at N=12, ~25% effective-alpha loss).
- **abstain-gate-after-light-usage**: Gate is scale-dependent and already always-open at 133 tools: 66/66 informative before AND after (min span .2175 to .2099), family evals min .2803; the 4-tool micro-corpus measures span .0990 and abstains; light usage adjusted 2 of 3 tools (108 to 2, 74 to 3) while the 3-success tool learned nothing and drifted 102 to 108 from sibling adjustments.

**Findings:**
- [HIGH · unverified] Four mislabeled successes hijack a need family's #1 slot and real failures cannot undo it *(proposes change)*
- [MEDIUM · unverified] Anti-monotone negative feedback: more failure evidence produces WEAKER push-away (centroid-norm artifact) *(proposes change)*
- [MEDIUM · unverified] Failures are nearly inert in draft ranking; 'learns from outcomes' is in practice 'learns from successes' *(proposes change)*
- [MEDIUM · unverified] Single-centroid OATS destructively interferes on multi-purpose tools; learning one mode pushes the other below baseline *(proposes change)*
- [MEDIUM · unverified] The 0.15 dense-abstain gate never fires at realistic corpus scale — its protection exists only for tiny rosters *(proposes change)*
- [LOW · unverified] All OATS dynamics are a cliff at exactly 4 positives — nothing at 3, everything at 4 *(proposes change)*
- [INFO · unverified] runOats is exactly idempotent — drift is impossible by construction
- [INFO · unverified] Post-OATS hybrid rank trails pure-dense rank on zero-overlap needs; recovery overshoots baseline

**Surprises:** The 0.15 dense-abstain gate — a headline safety mechanism validated in dense-live on 4 tools — never fires even once at 133-tool scale: minimum span across all 66 needs is 0.2175 with base (supposedly noise-level) MiniLM vectors, so the dense channel governs every single draft from day zero. · Negative feedback is anti-monotone: one failure pushes a poisoned tool to rank 7.3 but six diverse failures 'improve' it back to 5.0, because meanVec of diverse unit vectors shrinks in norm (1.0 -> .5547) and silently scales beta down; repeating the same failure adds exactly zero. · Learning one legitimate use-mode of a tool made it WORSE than never learning at all for its other mode (fetch__fetch SCRAPE rank 5.0 -> 10.7 after 6 POLL successes) — adj fully replaces base, discarding generic semantics. · runOats x30 produced bit-identical vectors (all deltas exactly 0.0) — OATS is a pure function of base+outcomes, so 'drift' and 'oscillation' are structurally impossible, which also means a poisoned adjustment can never decay on its own within the 90-day window. · Everything gates on the same cliff at 4 outcomes: a tool with 3 successes shows zero learning (and can even drift backward as siblings learn), then jumps from rank ~70 to top-3 overnight at the 4th.

## lexical-edges
- **A. Tokenizer morphology (15 tools x 6 variants)**: base/snake/hyphen identical (found 15/15, MRR 0.900); camelCase finds the tool 0/15 times (MRR 0.000, both query->snake and word-query->camel-name directions dead: 'print env' returns 0 rows for printEnv); plural flip drops hit@1 0.80->0.40 (MRR 0.521); verb form MRR 0.689, with draft('committing',5) returning five fs read/write tools and zero git tools.
- **B. Stopword pollution in the visible top-5 (66 needs, k=5)**: 17/66 needs (25.8%) carry a stopword-only wrong tool in top-5 (40/330 slots; extended set 29/66 = 43.9%, 74/330 slots); in 7/66 needs the contaminant outranks every primary tool, including rank-#1 slots with lexScore 1.0 (git__git_diff_staged #1 for 'show me the stack trace for that production error' matching only [for,that]).
- **C. Query-safety fuzz (206 hostile inputs)**: 0 throws, 0 raw-SQL-level errors, integrity ok (133/133 rows), no OR-term limit up to 30,000 terms, draft p50 0.03ms / p95 0.08ms / max 11.27ms at 5000 unique tokens — the [a-z0-9]{2,} sanitizer alone is injection-proof by construction.
- **D. Normalization edges + sanitizer over-strip**: The min-max+score>0 combo always discards the worst genuine lexical match: draft('csv',2) returned [export_csv, rotate_logs] while the only other csv tool was displaced to #5 behind three unrelated rated tools; 4/4 real needs with 2-5 matches had their worst match dropped from top-5; 'e-mail'->[mail] and 'café'->'caf' find 0 docs saying email/cafe; span==0 keeps all candidates at score 1.

**Findings:**
- [HIGH · CONFIRMED/CONFIRMED] Stopword-only matches put wrong-source tools into the visible top-5 for 25.8% of needs, sometimes at #1 *(proposes change)*
- [HIGH · CONFIRMED/CONFIRMED] No stemming: plural/verb-form phrasings crater ranking, and 'committing' returns five file-read tools *(proposes change)*
- [MEDIUM · CONFIRMED] camelCase is lexically unreachable in both directions (0/15), including real camelCase-named MCP tools *(proposes change)*
- [MEDIUM · CONFIRMED] min-max + score>0 filter always silently discards the worst genuine lexical match; with 2 candidates that is half the signal *(proposes change)*
- [MEDIUM · unverified] Recency filler is indistinguishable from real matches in the draft response, and zero-signal needs get 5 arbitrary fs tools presented as 'the best' *(proposes change)*
- [INFO · unverified] Query-safety verified: 206 hostile inputs, zero throws, zero hidden SQL errors, no corruption, no DoS surface
- [LOW · unverified] Sanitizer over-strips: accents mangle to garbage prefixes, hyphen splitting orphans short fragments, single digits vanish *(proposes change)*

**Surprises:** fs__list_allowed_directories is a serial rank-#1 stopword magnet — its description ('Returns the list of directories that this server is allowed to access') is so function-word dense that it tops the draft for 5 unrelated needs ('who is in this workspace', 'how big is that log file', 'what's on my plate this sprint', ...). · Recency filler currently HELPS the benchmark (draft hit@5 0.621 vs raw lexical 0.591) purely because fs read tools happen to be inserted first in the corpus — accidental luck that would invert with a different source order, and it makes camelCase queries look 13% successful when they match nothing. · No FTS5 OR-term limit exists up to 30,000 quoted terms in this SQLite build (3.53.2) — the anticipated 'silent lexical death on pasted-junk needs' cliff does not exist; a 5,000-token need costs only 11.27ms. · The store's try/catch around the FTS query is dead code in practice: raw-SQL probing of all 206 hostile inputs produced zero errors for the catch to swallow — safety comes entirely from the [a-z0-9]{2,} sanitizer. · min-max normalization crowns pure-stopword matches with a perfect lexScore of 1.0 whenever they are the only matches — the score the router surfaces is relative to a possibly-garbage candidate set, not absolute relevance.

## token-economics
- **live-receipt (A)**: 23 real tools: 5,509 est-tokens direct vs 866 via draft+call+1 draft = -84.3% (transparent list 4,988; five static 203; draft wire mean 662.9, p95 803); break-even 8.0 drafts; transparent list drops title (23/23), annotations (14/23), execution (23/23) = 2,150 chars.
- **corpus-133 (B)**: 133 tools: >=4,408 est-tokens direct (stub schemas, lower bound; 5,843 with 23 real schemas grafted) vs 501 via one draft = -88.6% (-91.4% vs hybrid); draft mean 297.5t; break-even 14.1-19.0 drafts; sanity hit@5 0.773.
- **tokenizer-honesty (C)**: chars/4 bias spans -37.4% (vs WordPiece on compact JSON) to +26.5% (vs legacy-Claude BPE on prose) - outside +/-15% in both directions; yet the savings headline is robust: -84.3% (chars/4), -88.7% (MiniLM), -83.3% (legacy-Claude BPE); break-even 7.5-12.5 drafts.
- **pretty-print cost (C2)**: Pretty-printing costs +53.3% real tokens on the legacy-Claude BPE (623.5 vs 406.8 mean) and +46.1% in chars/4 terms; MiniLM reports 0.0% because BERT normalization erases whitespace (WordPiece counters are blind to this waste).
- **trim-depth (D)**: d1 mean card 47.9t vs full 91.2t; 11/23 real tools (48%) lose argument structure (14/14 items keys, 26/26 prop descriptions, 4/4 defaults, 6 nested required dropped); depth-2 restores structure for +13.1% on affected cards (+6.4t each); wide200 synthetic yields an 1,800t card at EVERY depth >=1 (no width guard).

**Findings:**
- [MEDIUM · unverified] Transparent mode's tool list silently strips MCP metadata (title, annotations incl. readOnlyHint, execution) *(proposes change)*
- [MEDIUM · unverified] Receipt's '+/-15%' accuracy label on chars/4 is not defensible as a general claim *(proposes change)*
- [MEDIUM · unverified] Draft responses are pretty-printed, burning ~half again the marginal per-draft token cost on BPE tokenizers *(proposes change)*
- [MEDIUM · unverified] trimSchema depth-1 erases argument STRUCTURE (not just prose) for 48% of official-server tools *(proposes change)*
- [INFO · unverified] The public '~85% token cut' headline is real and tokenizer-robust at measured scales *(proposes change)*
- [INFO · unverified] fs and memory servers publish an 'execution' field on every tool that CapabilityEntry does not model

**Surprises:** MiniLM's WordPiece pipeline is completely blind to pretty-printing (0.0% overhead measured on 14 real wires) while the legacy-Claude BPE pays +53.3% for the same bytes - BERT normalizers erase whitespace, so a WordPiece counter silently hides a real class of token waste. · Roster's transparent tool list is 9.5% SMALLER than the direct connection it proxies, despite adding fs__/memory__ namespace prefixes - because it strips title/annotations/execution metadata; 'transparent' currently means 'transparent calls, lossy list'. · The chars/4 heuristic lands inside its advertised +/-15% only in one regime: legacy-Claude-BPE-on-JSON (+7..+14%); it is -35% off for WordPiece on the same payloads and +27% off on prose - the accuracy label encodes a tokenizer-family assumption nobody wrote down. · Depth is the wrong knob for schema width: the 200-property synthetic costs 1,800 tokens per card at EVERY trim depth >=1 (depth-1 = depth-6), so one pathological tool in a draft would out-weigh the entire 133-tool corpus list. · Real servers double the per-draft price relative to the lab corpus (662.9 vs 297.5 est tokens, 2.2x) because trimmed real schemas plus 240-char description truncations are much heavier than stub cards - savings percentages computed on stub corpora quietly flatter the draft path's marginal cost, though the headline still holds.

## proxy-tax
- **a: draftCandidates latency vs roster size**: Lexical p50 0.036→0.117ms and hybrid p50 0.047→1.104ms from 10→500 tools (hybrid 9.4x lexical at 500 because loadVecs deserializes every vec per draft); warm MiniLM need-embed adds 0.883ms p50.
- **b: CALL_TOOL overhead through the real middleman**: Middleman adds +0.19–0.22ms p50 (+0.23–0.45ms p95) per 1KiB call and +0.64–0.69ms at 64KiB (payload-proportional, both modes); direct baseline 0.165–0.171ms p50, drift-checked.
- **c: cold boot → first draft with 133 real tools**: Boot→first draft 0.17–0.29s (draft served lexically, never blocked on the model); dense ready 3.3s with warm-cache Gemma but serve RSS jumps from ~90MB to 1.73–1.93GB, and a warm-DB reboot re-embedded all 133 vecs (zero reuse), paying the 3.2s again.
- **d: coach.db growth and maintenance at 10k outcomes**: 3.16MiB at 10k outcomes (~266B/outcome, insert p50 34µs), draft latency degrades only 0.075→0.098ms lexical / 0.303→0.391ms hybrid, recomputeRatings 3.8–5.5ms, runOats 32–37ms adjusting all 133; unique need_vecs cost 2,138B each and are never pruned.

**Findings:**
- [MEDIUM · unverified] Default dense rung holds 1.7–1.9GB RSS per client session on ≥8GiB machines *(proposes change)*
- [MEDIUM · unverified] Warmup backfill re-embeds the entire roster every session — warm DB gives zero speedup *(proposes change)*
- [INFO · unverified] VERIFIED: the middleman's latency tax is sub-millisecond end to end
- [LOW · unverified] Hybrid draft cost grows linearly with roster size (full vec table re-read per draft) *(proposes change)*
- [LOW · unverified] outcome and need_vec grow unbounded (no pruning; 2.1KiB per unique need) *(proposes change)*
- [INFO · unverified] Maintenance and 10k-outcome scale are non-issues

**Surprises:** Warm-cache Gemma dense-readiness took only 3.3s on the M5 — but the RSS cost (1.73–1.93GB per serve process) is the real tax, not the wait. · A warm coach.db gives ZERO dense-warmup speedup: all 133 vectors were re-embedded and rewritten on reboot (updated_at proof), so the backfill cost is per-session forever, not once. · The 64KiB payload roughly triples the per-call overhead vs 1KiB (+0.65ms vs +0.21ms) — the proxy tax is serialization-proportional, though still sub-ms. · My first RSS probe was wrong in a way worth recording: pgrep matched a stale/sibling `roster serve` (pid 62703) instead of my child, reporting an absurd 4.2MB; results-b RSS was invalidated and superseded by transport.pid-based numbers in (c) — a live example of why 'measure the process you actually spawned' matters. · MiniLM on this machine is far cheaper than the architecture seems to budget for: 500 real tool-card embeds in 676ms and 0.88ms per need — the dense rung's latency cost is essentially free on MiniLM, while the auto-selected Gemma path costs ~55ms per draft and ~20x the RAM.

## concurrency-stress
- **8-process mixed-op contention on one coach.db**: 1600/1600 ops OK, 0 thrown errors of any code, 0 lost committed outcomes (640=640), integrity ok, 0 FTS orphans; worst single op 220.9 ms (recordOutcome) vs 5000 ms busy_timeout, per-op p50 0.11-0.81 ms, all 8 workers overlapped inside a 489 ms window.
- **Racing serve-boot sync cycles with divergent rosters**: A's 10 exclusive capabilities were deleted at the start of 10/29 cycles (100 id-losses) and B's at 8/29 (80), vectors deleted with them every time; final DB (B pruned last) had all 10 A-only tools + vectors gone while 'serve A' was live; 0 SQLITE errors - the keepSeenSince guard held for overlapping boots (~2/3 of cycles) and failed for every sequential interleaving.
- **kill -9 crash safety, 28 rounds incl. forced WAL spill**: 28/28 rounds integrity_check ok, 0 torn transactions visible, 0 durability violations (5 rounds showed exactly +1 unacked committed row - ACK pipe died at kill, expected); methodological catch: all 10 small-row mid-tx kills died with a 0-byte WAL (dirty pages never left RAM), so only the forced-spill rounds truly exercised WAL rollback - which passed 8/8.
- **saveConfig torn-write and lost-update races**: 1363/2400 saveConfig calls (56.8%) crashed ENOENT on the shared fixed tmp name, 109/5760 reads (1.9%) saw torn/empty JSON, 2/20 racing-add rounds left roster.json PERMANENTLY corrupt (valid JSON + stray bytes, loadConfig throws => serve boot crashes until hand-repair), 10/20 rounds a writer that reported success had its change silently lost while in 7/20 a crashed writer's data survived instead.

**Findings:**
- [HIGH · unverified] saveConfig's shared fixed tmp path produces torn and permanently corrupt roster.json under concurrent writers *(proposes change)*
- [HIGH · unverified] 56.8% of concurrent saveConfig calls crash with ENOENT *(proposes change)*
- [MEDIUM · unverified] Config write acknowledgment is meaningless under races: acknowledged writes lost, crashed writes survive *(proposes change)*
- [MEDIUM · unverified] keepSeenSince prune guard is a time window, not roster awareness: sequential divergent boots delete a live sibling's capabilities and vectors *(proposes change)*
- [INFO · unverified] coach.db multi-process contention and crash safety verified clean under real load
- [INFO · unverified] Small-transaction kill tests do not test WAL recovery at all (0-byte WAL at kill)

**Surprises:** A saveConfig caller that returns SUCCESS can have its change silently discarded while a caller that CRASHED with ENOENT is the one whose data survived (7/20 rounds) - the shared tmp file means processes publish each other's bytes. · Two ordinary racing 'roster add' operations left roster.json PERMANENTLY corrupt 10% of the time (2/20), turning every future serve boot into a crash until manual repair - the failure the tmp+rename comment claims to prevent. · All 10 small-row mid-transaction kill -9s left a 0-byte WAL: the entire uncommitted transaction died in user-space pager cache, meaning naive crash tests validate nothing; WAL rollback only became testable after forcing multi-MB cache spills (which then passed 8/8). · Zero SQLITE_BUSY/SNAPSHOT errors anywhere across all experiments (1600 contended ops + 60 racing sync cycles) - busy_timeout=5000 absorbed everything with a worst single-op stall of only 220.9 ms. · The torn-file mechanism was reconstructable byte-for-byte from captures: both writers O_TRUNC-open the same tmp before either writes, so the shorter later write leaves the longer earlier write's tail (e.g

## sync-eject-fuzz
- **A: 100 valid-but-weird configs, byte-for-byte cycle**: 100/100 byte-identical after every completed cycle; 90 synced+restored, 4 already-synced, 6 sync THREW (utf8-bom x3, scalar-top x3, configs untouched); 3 array-top cases claimed synced without ever writing a roster entry.
- **A2: real dist/bin.js fleet-sync abort**: BOM on client 1: exit 1, 0/4 clients synced, raw 'Unexpected token' error with no filename; BOM on client 3: 2/4 synced then abort, openclaw never synced.
- **B: 30-sequence operation fuzz (420 ops)**: 0 violations in 420 ops across all 5 invariants (an initial 135 I1 flags all traced to users deleting their own never-synced configs -- harness model artifact, verified by replay); `latest` pointer always correct but has zero readers in src.
- **B3: same-millisecond double-sync collision**: Pristine destroyed: 1 backup dir (second sync overwrote `original` in place), eject reported 'restored' but returned the post-edit roster-pointing bytes; true original unrecoverable. API-level only (CLI cannot re-sync within 1ms).
- **C: backup tamper matrix (9 cases)**: All `original`-byte tampering refused loudly (BACKUP INTEGRITY FAILURE, config untouched), but one corrupt byte in the PRISTINE backup's manifest.json in a 2-backup era produced a silent wrong restore: action 'restored', bytes = backup #2 (user-edited, still roster-pointing), silentWrongRestore=true; baseline without tamper restored true pristine.
- **D: crash-window enumeration (SIGKILL + torn writes + fs-error injection)**: 0/18 kill windows unrecoverable (9 sync + 5 torn + 4 eject; config always either original or fully-written with valid backup), but fs errors at ops 1-3 (import-save phase) are swallowed: sync completes, reports 'synced', config rewritten to roster-only while roster.json was never written -- user's imported servers routed nowhere with no warning.
- **E: micro-probes (array churn, comment fate)**: Array config: 4x 'synced', 4 backup dirs, final config `[]\n` with no roster entry ever written (false success + churn); comments are stripped from the live config during the era but restored byte-perfect on eject.

**Findings:**
- [HIGH · unverified] One corrupt byte in a pristine backup's manifest.json makes eject silently restore the WRONG backup *(proposes change)*
- [MEDIUM · unverified] A failing fs call during the import-save phase is swallowed: sync reports 'synced' while the user's servers are routed nowhere *(proposes change)*
- [MEDIUM · unverified] One malformed client config aborts the whole `roster sync` fleet run with an anonymous error *(proposes change)*
- [MEDIUM · unverified] Two syncs of the same client within one millisecond destroy the pristine backup in place *(proposes change)*
- [LOW · unverified] Corrupt manifest in a single-backup era strands the user: 'no backup recorded' while pristine bytes sit intact on disk *(proposes change)*
- [LOW · unverified] Top-level-array config: sync claims 'synced' forever, never writes a roster entry, and accretes one backup dir per run *(proposes change)*
- [LOW · unverified] Exported syncClient mis-writes the 6 non-write clients (bin.ts guards them today) *(proposes change)*
- [INFO · unverified] The byte-for-byte trust claim measured TRUE everywhere the cycle completes; crash atomicity holds in all 18 kill windows
- [INFO · unverified] Misleading 'refused-modified' after crashes, write-only `latest` pointer, era-state comment loss, self-referential integrity

**Surprises:** The biggest finding came from a harness bug: my injected CrashError at ops 1-3 kept getting 'swallowed' -- turned out syncClient's catch-all around the import step eats real fs errors too, completing the sync with the user's servers routed nowhere · manifest.sourcePath is an absolute path, which invalidated my first snapshot-based recovery probes: ejecting a COPIED home wrote the restore back into the ORIGINAL home directory (cross-home write) · The integrity asymmetry is perfectly sharp: one flipped byte in `original` -> loud refusal; one flipped byte in the adjacent manifest.json -> silent wrong restore that still points the 'ejected' user at roster. · Byte-for-byte held even for the nastiest inputs -- 1MB configs, lone-surrogate escapes, CR-only line endings, TOML with inf/nan/datetimes -- 100/100 with zero tmp-file litter. · JSON.stringify silently dropping properties set on arrays turns a top-level-array config into an infinite 'synced' loop that never actually installs roster and mints a new backup dir every run.

## classifier-realworld
- **real-failure scenario matrix (part a)**: Only 4/14 classifier-reaching scenarios match spec intent: all 4 drift shapes -> hard_fail:protocol (client-side SDK McpError -32602/-32600, schema_drift_suspect unreachable), kill-mid-call -> hard_fail:protocol (McpError -32000) instead of hard_fail:transport, agent-malformed args -> attributable tool_fail:schema, unknown tool -> tool_fail:other (isError result, not JSON-RPC error), and unbounded BackendManager.connect to a wedged process blocks 60,006ms.
- **precedence + attributability fuzz (part b)**: 0/500 precedence violations, 0/500 kind-order violations, every emitted class attributable and both markers correctly non-attributable (1000/1000 clean).
- **realistic error texts over real isError wire (part c)**: 11/46 (23.9%) misclassified, all auth-direction: bare /token/ flips OpenAI 429 'tokens per min' and 'Unexpected token < in JSON' to auth, \bauth flips GitHub's verbatim rate-limit text ('Authenticated requests') to auth, while Slack invalid_auth/not_authed, AWS 'Signature expired' and the fs server's real 'Access denied - path outside allowed directories' all miss auth.
- **proposed ruleset evaluation (part c2, repo untouched)**: Misclassifications drop 11 -> 0 (23.9% -> 0%) with 0 regressions on the 35 already-correct texts; caveat: tuned on this corpus, needs held-out validation.

**Findings:**
- [HIGH · unverified] Output-schema drift detection is structurally dead on the real wire *(proposes change)*
- [MEDIUM · unverified] Real transport death mid-call is classified hard_fail:protocol, not hard_fail:transport *(proposes change)*
- [MEDIUM · unverified] Methodology section-8 fairness leaks: agent-malformed args become attributable tool_fail:schema on the real wire *(proposes change)*
- [MEDIUM · unverified] Kind heuristics misclassify 23.9% of realistic error texts, all in the auth direction *(proposes change)*
- [MEDIUM · unverified] roster serve blocks 60s per wedged backend before its per-backend try/catch resumes *(proposes change)*
- [INFO · unverified] hard_fail:protocol has become a taxonomy soup while genuine JSON-RPC errors moved into tool_fail:*
- [INFO · unverified] Precedence and attributability verified clean under fuzz (positive result)

**Surprises:** The MCP SDK client (1.29.0) validates structured output ITSELF from listTools-cached schemas and throws before the router's own drift check can run - S10d proved the throw happens even when backends.ts is not given the schema, making ROSTER's entire drift-detection path (violatesOutputSchema -> schema_drift_suspect -> drift event) unreachable dead code on the real wire. · A real server crash mid-call does not look like a transport error to the code: it arrives as McpError -32000 'Connection closed', which the 'McpError means protocol' branch swallows - and because SUGGESTION_CLASSES keys on hard_fail:transport, the Sixth Man goes silent exactly when a server dies. · Modern SDK servers fold JSON-RPC -32602 errors INTO isError results ('MCP error -32602: ...' as content text): 'Tool not found' and input-validation failures never reach the protocol branch at all, so the agent's own malformed args become attributable tool_fail:schema rows that will ding the tool's public Wilson rating. · GitHub's verbatim rate-limit message classifies as auth purely because it contains the word 'Authenticated', and reading a missing file named auth-tokens.txt flips ENOENT to tool_fail:auth via the path echoed in the error text - file names change outcome classes. · npx spawn of a nonexistent command fails in 9ms, but a live process that never speaks MCP blocks the production connect path for exactly 60,006ms (SDK default), sequentially per backend in roster serve.

## playbook-trust
- **a-parse: SKILL.md parse robustness (62 fuzz cases)**: 0 crashes, 0 hangs across 62 cases (2MB body parses in 0.25ms, YAML bomb rejected in 5ms), but 2 BOM cases silently drop the entire frontmatter and 3 cases silently drop the skill.
- **b-trust: trust-scan FP/FN matrix (15 malicious + 15 benign real skills)**: Status-level 40% FN (6/15 malicious scored fully clean) / 53.3% FP; content-level 53.3% FN (8/15 payloads undetected) / 33.3% hard FP (5/15 benign tripped a content rule).
- **c-openclaw: injection-cost formula verification (10 real files)**: Zero drift: real total = independent total = 2697 chars, 0/10 per-skill mismatches, estTokens=675=ceil(2697/4), body-excluded and empty-list=0 both hold.
- **d-symlink: symlinked skill dir safety**: Silent-and-safe: no throw, scan in 3.1ms, symlinked dir + dangling link skipped, resource symlink loop did not recurse (no hang).

**Findings:**
- [HIGH · unverified] A UTF-8 BOM silently voids the entire SKILL.md frontmatter *(proposes change)*
- [MEDIUM · unverified] Trust scanner never reads the skill description — the highest-value injection surface is a blind spot *(proposes change)*
- [MEDIUM · unverified] Trust scanner counts bundled scripts but never reads their contents — curl|bash and base64-exec inside a script are invisible *(proposes change)*
- [MEDIUM · unverified] Measured trust-scan rates: 40% of malicious skills score a clean 'ok'; 33% of benign skills trip a content rule *(proposes change)*
- [MEDIUM · unverified] Trivial delimiter slips and tab-indented YAML silently drop metadata or the whole skill *(proposes change)*
- [INFO · unverified] OpenClaw injection-cost formula has zero drift from its documented accounting
- [INFO · unverified] Symlink handling is silent-and-safe (no crash, no loop) — positive verification
- [INFO · unverified] Parser is crash/hang/ReDoS-safe across 62 adversarial inputs — positive verification

**Surprises:** The status-level trust FN (40%) is actually OPTIMISTIC vs the content-level FN (53%): m10/m12 look 'caught' because bundling any script trips the generic bundled-scripts advisory, but the real curl|bash / base64-exec inside those scripts is never read, so a 'review' status can still hide the actual malware. · A prompt-injection-DEFENSE skill (b11) that quotes 'ignore all previous instructions' to teach recognition is flagged as an injection attack — the regex scanner cannot distinguish teaching-about from doing. · A single invisible BOM byte fully breaks parsing, and String.trim() strips that BOM off the body afterward, so the corrupted skill looks superficially normal (just missing its name/description) with no trace of the cause. · The description field — the exact text OpenClaw injects into every prompt and that retrieval indexes — is never scanned by trustScan at all; injection there is a total blind spot. · The OpenClaw char formula was exactly zero-drift (2697=2697), a clean pass — the interesting caveat is only that 'EXACT' is unfalsifiable in-repo because OpenClaw's real <available_skills> template isn't vendored.

## combine-adversarial
- **(a) determinism**: 3 reps 8/8 pass, pass/stage/detail/signed IDENTICAL (per-task diff: NONE); environmentDigest stable 192ca160ad31db34…; only latencyMs+generatedAt vary.
- **(b) mutation-test the 8 verifiers**: Baseline 8/8 pass; 18 sabotages → 14 caught, 4 FALSE-PASSES: create-dir wrong-type-file, create-dir case-flip, write case-flip, write unicode-NFD.
- **(c) false-fail hunt**: All 5 shapes 8/8 pass, 0 false-fails; longest root 200 chars still clean.
- **(d) containment**: 8 escape attempts all contained (runner-guard: 'path escapes sandbox'; server: 'Access denied - path outside allowed directories'); ~ stays literal INSIDE sandbox; filesystem scan: 0 bytes written outside any sandbox.
- **(e) timeout / misbehaving server**: hang-on-connect → transport/'connect timeout' @17.0s (15s CONNECT_TIMEOUT+spawn); hang-on-call → invoke/'MCP error -32001: Request timed out' @2.6s; both suites returned; both children killed.

**Findings:**
- [HIGH · unverified] fileExists false-passes on a regular file: create_directory is certifiable by a server that writes a FILE *(proposes change)*
- [MEDIUM · unverified] File verifiers inherit host-filesystem case/unicode path-equivalence → 3 false-passes on macOS (immune on Linux) *(proposes change)*
- [LOW · unverified] resultContains("size") and other generic substrings are weak verifiers *(proposes change)*
- [INFO · unverified] Containment is layered: runner guards setup+verify only; invoke args are delegated to the server; guard is lexical
- [INFO · unverified] Determinism, digest stability, timeout classification and child reaping are solid

**Surprises:** Host-filesystem behavior silently leaks into the verifiers: on macOS/APFS (case-insensitive + unicode-normalizing) the fileExists/fileEquals checks false-pass case-variant and NFD-variant end-states — a truth invisible to unit tests and to any case-sensitive Linux-only CI, exactly the real-environment-only class the swarm exists to catch. · fileExists genuinely cannot distinguish a directory from a regular file, so the 'create a directory' task is certifiable by a server that writes a plain file — and this holds on EVERY OS, not just macOS. · The 15s CONNECT_TIMEOUT actually took 17.0s of wall time end-to-end (npx/node child spawn + transport close overhead) — the design fires correctly, but the real number is ~2s over the nominal timeout. · Containment is quietly two-layered: the runner's path-guard only protects setup+verifier paths — invoke args are entirely the server's responsibility, so the server-under-test IS the containment boundary for tool calls, and the runner never independently scans where the server wrote.

## drift-sim
- **drift-sim 30-day lifecycle conformance**: 48/48 assertions pass: dwell honored to the hour (quarantined at +1h/+6h/+23h, cleared at +25h; 48h-gap single re-sight clears; flap restarts dwell with 2 chained events), 10 drift events with contiguous hash chains, CLI unquarantine exits 0 cross-process — but remove/re-add produced 0 drift events for a changed definition, voided an active 22h-old dwell, and all 7 quarantine clears happened with zero Combine runs despite spec's 'pending re-Combine'.
- **drift-sim signature-change state transfer**: Rating (4/4, wilson .510) transfers byte-identical to the repurposed tool; the served vector stays cos 0.902 to the OLD signature through warmup re-embed (adj survives base rewrite by design) and even after the next nightly OATS keeps a permanent old-semantics pull (cos 0.722 old vs 0.791 new, from the 90-day outcome window); the old-semantics need 'read the contents of a file stored in a github repository' drafts the deploy tool #1 (dense governing, span 0.413) ahead of the correct fs__read_file at #3.

**Findings:**
- [HIGH · unverified] Remove/re-add fully bypasses drift detection, the public drift ledger, and an active quarantine dwell *(proposes change)*
- [MEDIUM · unverified] Runtime schema_drift_suspect never raises a drift event (handoff §6.2 rule 3 not implemented) *(proposes change)*
- [MEDIUM · unverified] Spec says quarantine lifts 'pending re-Combine'; implementation lifts on 24h wall-clock dwell with no Combine involvement *(proposes change)*
- [MEDIUM · unverified] Full signature change carries the predecessor's rating, OATS vector, and outcome history into a semantically different tool — old-semantics needs still draft it #1 *(proposes change)*
- [LOW · unverified] outputSchema changes are invisible to connect-time drift AND never persisted to the store *(proposes change)*
- [INFO · unverified] Dwell lifecycle verified exactly as STATUS-FOR-MO describes; quarantine is per-tool (not per-server) and gates drafting only

**Surprises:** The OATS 'adj ghost' outlives everything: after a full signature change, warmup re-embeds the base to the new text (cos 1.0) yet the draft-facing vector is untouched (adj preserved by design, cos 0.902 to the OLD description), and even the next nightly OATS bakes the old semantics back in from the 90-day outcome window (cos 0.722 old vs 0.791 new) — quarantine expires long before the learned state does. · An ACTIVE quarantine dwell is voided by removal: a tool quarantined 22 hours earlier came back fully active the moment it was pruned and re-listed with the drifted definition — the dwell is not sticky across a remove/re-add cycle, and no drift event marks the return. · My inspection harness initially reproduced the exact Buffer-pool byteOffset bug that packages/coach/src/util.ts blobToVec's comment warns about (reading pooled memory as vector floats gave cos 0.13 where truth was 1.0) — the repo's defensive comment is empirically earned. · The dwell mechanics are more precise than the spec text implies: quarantined at +23h, cleared at +25h, a 48h-unseen tool clears on its first re-sight (dwell measured from the drift EVENT, not from re-sight streaks), and a flap-back restarts the clock — all verified against the real store with zero source modification, using only the public now parameters.

## embed-torture
- **A. Queue serialization under 50-way concurrency**: 50/50 resolved bitwise-identical to serial refs (maxAbsDiff 0); 28/28 interleaved rows map to the right text; concurrent wall 81.4ms vs serial 77.1ms; 0 unhandled rejections
- **B. dispose() during flight (warm, cold, double, post-dispose)**: Warm: 20/20 fulfilled with valid vectors (cos 1.0 vs pre-dispose ref); cold: 5/5 clean 'embeddings provider disposed' rejections; post-dispose embed rejects clearly; double-dispose no-op; 0 unhandled rejections, 0 uncaught exceptions
- **C. Re-warm cycles x5 + 12-round leak isolation (C2)**: No leak: 12 create/dispose cycles plateau at ~215MB (slope -0.45MB/round) exactly matching the never-disposed arm's 216MB (slope +0.01) — but dispose reclaims only ~1.8MB mean of the ~164MB model footprint at RSS level (baseline 51.8MB); warm reload costs 38ms
- **D. Input torture**: All string cases yield unit-norm 384-d vectors (norm 1.000000, 0 NaN/Inf); empty batch [] rejects cleanly ('text array must be non-empty') and the queue survives; 500 identical texts -> 500 bitwise-identical rows in 160.7ms; 100KB text embeds in 50.8ms via silent 512-wordpiece truncation, no hang
- **E+F. Chunked-batch equivalence + cross-process determinism**: 0/133 bitwise-identical (cosine min 0.99781, max component diff 0.0121); 0/20 top-1 flips but 3/20 top-5 order changes; batch speedup only 1.25x (1.34 -> 1.08 ms/text); hashes are bit-identical across processes per shape — batch composition is the sole numeric instability

**Findings:**
- [MEDIUM · unverified] Embedding vectors are batch-shape-dependent: serve's batch-16 stored vecs never match single-text re-embeds, with measurable top-5 jitter *(proposes change)*
- [LOW · unverified] dispose() reclaims ~0 RSS for MiniLM — the '~300MB reclaim' rationale is not RSS-observable for this model; real verified benefit is no-accumulation *(proposes change)*
- [INFO · unverified] Concurrency queue and dispose lifecycle are genuinely solid under real ONNX load
- [INFO · unverified] Input hardening holds: 5,342 real vectors with zero NaN/Inf; degenerate inputs give unit vectors or clean errors — but empty string silently embeds
- [INFO · unverified] Batch-of-16 embedding gives only 1.25x throughput on MiniLM

**Surprises:** 0/133 vectors bitwise-identical between batch-of-16 and single-text embedding of identical texts — yet determinism is EXACT (bitwise) within any fixed batch shape and across process restarts; batch composition is the sole source of numeric instability in the whole charter. · The apparent +22.3MB/5-cycle RSS 'leak' in Part C vanished at 12 cycles: both the cycled and never-disposed arms converge on the same ~215-216MB plateau · dispose() reclaims essentially zero RSS for MiniLM (~1.8MB of ~164MB) despite performing real ONNX disposal — the fix's true property is memory reuse across sessions, not release to the OS. · A 100KB input embeds in 50.8ms with silent truncation to 512 wordpieces — no hang, no error, and no warning that 99% of the text was ignored. · Empty string '' embeds to a perfectly valid unit-norm vector in 1.6ms — the provider will happily produce confident-looking geometry for literally nothing.

## ratings-math
- **wilson-vs-independent-reference**: Max abs diff 3.33e-16 over 5,017 pairs; 0 violations in 47,150 s-monotonicity and 2,000 n-scaling checks; output always in [0, 0.99962]; z=0 collapses to p-hat exactly; wilson(3,3)=0.43849 < wilson(84,100)=0.75580 as documented.
- **recompute-ratings-hand-table**: All 5 cases exact (wilson diff <= 9.7e-17; failure latencies never touched p50/p95; 9 explored rows invisible; soft_fail flip confirmed in SQL), but recomputeRatings('web') wrote unfiltered global stats (n=10,s=2 instead of category-true n=2,s=2) under 'web' for all 6 outcome-bearing capabilities, and a rating row persisted (n=1, w=0.20654) after its capability's attributable evidence emptied to 0 rows.
- **rated-fallback-determinism**: 5/5 non-empty and exactly rating-ordered at both roster sizes; 10/10 repeats, 3/3 rebuilds, and 10/10 file reopens identical; all-ties order = insertion (rowid) order and stable; backfill LIMIT limit+exclude.size seated the 5th-rated tool correctly at the exact off-by-one boundary with zero duplicates.
- **percentile-latencybucket-edges**: percentile: 108/108 agreement, empty->null, p50 of [1,2] = 1 (lower-median convention), p<0/p>100 clamp gracefully, NaN p -> null; latencyBucket: all finite boundaries left-closed/right-open, ms=4000 -> '>4000' (label says strictly greater), NaN -> '>4000' (worst bucket), and the function has zero runtime call sites.
- **worst-hit-displacement-scan**: 87.6% (85/97) of narrow token needs lose a genuine FTS hit from the k=5 draft; need 'write' returns sqlite__write_query + 4 unrelated firecrawl tools while literal match fs__write_file is absent; a single success (wilson 0.2065) re-seats it at #2.

**Findings:**
- [MEDIUM · unverified] Cold-start drafts drop a literal keyword match for 87.6% of narrow needs (zeroed-worst-hit x fallback interaction) *(proposes change)*
- [MEDIUM · unverified] recomputeRatings(category) silently ignores intent_cat: any non-'all' call writes mislabeled global ratings for every capability *(proposes change)*
- [LOW · unverified] Ratings never expire: a rating row survives after its capability's attributable evidence empties, and keeps ranking the tool in the fallback *(proposes change)*
- [INFO · unverified] Wilson lower bound is exact and property-clean on the built dist (trust-confirming)
- [INFO · unverified] recomputeRatings aggregation is exact against hand-computed tables; soft_fail/explored exclusion and class weighting verified by construction in real SQLite
- [INFO · unverified] Rated fallback is non-empty, exactly rating-ordered, and deterministic across repeats, rebuilds, and file-DB reopens (trust-confirming)
- [INFO · unverified] percentile matches nearest-rank reference 108/108; latencyBucket sends NaN to the worst bucket, mislabels ms=4000, and has zero runtime callers *(proposes change)*

**Surprises:** The e2 boundary test's diagnostics, not its assertion, exposed the biggest truth: a genuine lexical hit re-entered the draft via the FALLBACK channel because min-max normalization had zeroed it out of the scored set — scanning showed this displaces a real FTS match from the k=5 draft for 87.6% of narrow (2-5 hit) needs in a cold store. · recomputeRatings('web') doesn't just mislabel one capability — it bulk-writes unfiltered global ratings under the requested category for EVERY capability with outcomes, so a single stray call would fabricate an entire fake per-category leaderboard. · isAttributable looks like a no-op (it accepts every value of the OutcomeClass union) but measurably earns its keep against raw-SQL legacy rows: an injected class='weird_legacy_class' row was correctly excluded from n. · latencyBucket(NaN) returns '>4000' — a NaN latency would silently land in the WORST bucket — yet the function turns out to have zero runtime call sites, so the trap is currently unarmed. · The two formula paths for Wilson (repo centre/spread vs independent closed form) agree to 3.3e-16 over 5,017 pairs — better-sqlite3 round-tripping the REAL stored wilson_lb didn't add a single ULP of drift.

## identity-fuzz
- **A: namespacedId/parseNamespacedId round-trip fuzz + collision hunt**: 922/1000 round-trip; all 78 failures are one mechanism (source ending '_' or name starting '_' shifts the '__' split); 300 ids in the systematic hunt carry >=2 DISTINCT sanitized identities; sources sanitizing to '_' emit ids parseNamespacedId itself rejects (4/5 probes null).
- **B: BackendManager registry with real SDK servers (colliding names, reserved word, boot swap, prune)**: Suffixing and 'skill'->'skill-server' rename work; 'my-server'/'my_server'/'MY SERVER' stay distinct; but backends 'a_' and 'a' produce duplicate id a___b and the live call for one was answered by the other (1 misroute); my-server__run answered by PHYS-A in boot1 and PHYS-B in boot2; pruneMissing deleted skill-server__lookup_docs and my-server-2__run despite serve's protection set (counterfactual prunes 0).
- **C: trimSchema/toCard torture**: All cases terminate in <0.2ms (max 0.183ms) with no hangs; only null/undefined throw (guarded at internal call sites); 1000-entry enum survives the 'trim' at keptFraction 1.0 = 33,958 bytes per draft card while prose is capped at 240 chars; cyclic enum kills the card stringify but is unreachable via stdio and blocked at syncCapabilities via in-process transport.
- **D+D2: Ajv args_compatible matrix, $id poisoning, cache leak, live wire**: Live wire args_compatible = [true,false,false] for identical args (clean $id registers once then 'already exists' throws forever); fragment-$id is false from call one; draft-07 tuple items and draft-04 boolean exclusiveMinimum yield false-incompatible on valid args; prod pattern costs 42.9ms + ~0.3MB heap per validate on a 500-prop schema (0.031ms same-object baseline, ajv._cache 2008 entries after 2000 calls; small schema 0.19ms + 23.6KB); zero crashes — every throw caught, both live rigs alive after torture.
- **E: real npx server-filesystem under the reserved name 'skill'**: 14 real tools stored as skill-server__* (connect 694ms, real call returned the probe file in 2ms); next-boot prune with protection {skill} deleted 14/14 capabilities despite serve.ts printing 'its learned state is preserved'.

**Findings:**
- [HIGH · unverified] serve.ts prune protection uses config-name sanitization while capabilities are stored under post-rename/suffix keys — learned state is deleted despite the printed 'preserved' promise *(proposes change)*
- [HIGH · unverified] Cross-boundary namespace collisions: distinct (source,name) pairs collapse onto one id, and lookup routes calls to the wrong live backend *(proposes change)*
- [HIGH · unverified] $id-bearing alternate schemas poison args_compatible: true on first suggestion, false forever after, for identical args *(proposes change)*
- [HIGH · unverified] Boot-order identity swap: post-sanitization duplicate config names reassign tool ids to different physical servers across boots *(proposes change)*
- [MEDIUM · unverified] sixthMan Ajv path recompiles on every suggestion and grows ajv._cache unboundedly (never hits its own cache) *(proposes change)*
- [MEDIUM · unverified] Dialect false-negatives: valid draft-07/draft-04 args report args_compatible=false *(proposes change)*
- [MEDIUM · unverified] trimSchema passes entire enums through to draft cards: a 1000-entry enum keeps 100% of its 33,958 bytes *(proposes change)*
- [LOW · unverified] trimSchema(null/undefined) throws TypeError; sources sanitizing to '_' emit ids the parser rejects *(proposes change)*
- [INFO · unverified] Verified good: registry suffixing/reserved word, torture termination, and the no-crash gate all hold

**Surprises:** The charter's 'designed to collide' registry names (my-server / my_server / MY SERVER) do NOT collide — sanitization preserves case and underscore-vs-dash; all real collisions live on the underscore boundary instead, plus unicode-only names all collapsing to 'x'. · args_compatible is nondeterministic from the client's perspective: the identical failing call three times returned [true,false,false] on the wire — Ajv's $id registry turns a per-call destructure into cross-call state poisoning. · The prod validate pattern is worse than having no cache: ajv._cache grows exactly 1 entry per suggestion (2008 after 2000) while never once hitting, costing 612MB heap and 85.7s for 2000 fat-schema validates that a same-object baseline does in 62ms total. · A cyclic schema sails through tools/list and tool calls over InMemoryTransport (no serialization boundary anywhere in SDK client-server), yet the same cycle kills syncCapabilities via SQLite JSON stringify — the DB, not the protocol, is the actual cycle firewall. · serve.ts's own stderr line 'its learned state is preserved' is the exact claim the measurements falsify: a real filesystem server configured under the name 'skill' lost 14/14 learned capabilities on its first unavailable boot.

---
Totals: 100 findings across 15 charters; 10 reached at least one verifier verdict before the limit.
